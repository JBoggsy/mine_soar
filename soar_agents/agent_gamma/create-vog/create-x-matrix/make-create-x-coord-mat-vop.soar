sp {create-x-matrix*propose*create-vop-node
   (state <s> ^name create-x-matrix)
  -{(<s> ^top-state.svs.vsm.vog.node <node>)
    (<node> -^node-name
             ^op-name <> save-to-file)}
-->
   (<s> ^operator <op> + =)
   (<op> ^name make-create-x-coord-mat-vop
         ^size-x 640
         ^size-y 480)
}
