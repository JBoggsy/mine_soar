sp {create-vog*propose*create-Fv-matrix
   (state <s> ^name create-vog
              ^top-state.svs.vsm.vog <vog>)
  -{(<vog> ^node <node>)
    (<node> ^node-name |Fv matrix|)}
-->
   (<s> ^operator <op> + =)
   (<op> ^name create-Fv-matrix)
}
